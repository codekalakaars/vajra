[package]
name = "vajra"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
nix = { version = "0.29", features = ["sched", "mount", "user", "process", "signal"] }
libc = "0.2"
