[package]
name = "vajra"
version = "0.1.0"
edition = "2024"

[build-dependencies]
slint-build = "1.7"

[dependencies]
slint = "1.7"
clap = { version = "4", features = ["derive"] }
nix = { version = "0.29", features = ["sched", "mount", "user", "env", "process"] }
libc = "0.2"
